import {Theme} from './types';

export const appTheme = (): Theme => {
  const theme = {
    cornflowerBlue: '#569BF9',
    frenchPass: '#B1E2FC',
    shineGray: '#B9B9C3',
    white: '#ffffff',
  };
  return theme;
};
